# Pinned to versions known to work together. To upgrade, run `pip install -U <pkg>`,
# verify the suite still passes, then update the pin here.

# Workflow / agent framework
langgraph==0.6.11
langchain==0.3.29
langchain-community==0.3.31

# LLM clients
ollama==0.6.2
anthropic>=0.40.0,<1.0.0

# Data validation
pydantic>=2.0

# PDF + data
pdfplumber==0.11.8
pandas==2.3.3
openpyxl==3.1.5

# Image manipulation (used for auto-orientation correction)
Pillow>=9.1

# Frontend
streamlit==1.50.0

# Tests
pytest>=8.0.0,<9.0.0

# Token counting (OpenAI-compatible tokenization)
tiktoken>=0.7.0

# Image orientation / OCR helpers (optional but recommended)
pytesseract>=0.3.10

# MCP server (remote tool access to the extraction pipeline)
mcp==1.28.1
uvicorn==0.51.0

# Vector store for RAG-assisted CoA retrieval
chromadb==1.5.9
